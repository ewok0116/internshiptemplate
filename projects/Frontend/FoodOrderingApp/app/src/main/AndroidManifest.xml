<!-- Add this to your AndroidManifest.xml file (app/src/main/AndroidManifest.xml) -->
<!-- Add this line BEFORE the <application> tag -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ADD THIS LINE FOR INTERNET ACCESS -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- If you want to allow HTTP (non-HTTPS) connections, also add this: -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodOrderingApp"
        tools:targetApi="31">

        <!-- If you need to allow HTTP connections (for testing), add this attribute: -->
        <!-- android:usesCleartextTraffic="true" -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FoodOrderingApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>